#include <stdio.h>

/**
 * main- print phrase with proper grammar,
 * but the outcome is a piece of art,
 * return 0 if no errors, otherwise non-zero
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
